# Docker container and compose for my gallery project

## Usage
- `docker-compose up -d harrison` to build harrison_newborn gallery on an nginx server
- `docker compose up -d wedding` for the wedding gallery
